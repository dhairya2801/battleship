{
  "scripts": {
    "test": "jest",
    "build": "webpack",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "css-loader": "^7.1.1",
    "gh-pages": "^6.3.0",
    "html-webpack-plugin": "^5.6.0",
    "jest": "^30.2.0",
    "style-loader": "^4.0.0",
    "webpack-cli": "^6.0.1"
  }
}
